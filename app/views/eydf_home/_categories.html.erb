<div class="sidebar_box">
  <h3><%= t('.categories')%></h3>
  <ul class="tmo_list">
    <% @constants.each do |const|%>
      <% @blog_counts = EydConstant.count_by_sql("select count(*) from eyd_blogs blog where blog.constant_id = #{const.id} and blog.user_id=#{userId}")%>
      <li><%= image_tag("category_list.png")%><%= link_to const.category, {:action => :category_list, :id=> const.id}%>( <%= @blog_counts.to_s%> blogs)</li>
    <% end %>
  </ul>
</div>
